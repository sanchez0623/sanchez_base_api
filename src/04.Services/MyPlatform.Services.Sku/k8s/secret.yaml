# ==========================================================================
# 注意: 此文件仅为示例配置，包含占位符密码和密钥
# WARNING: This is an EXAMPLE configuration with placeholder secrets.
# 
# 在生产环境中不要直接使用此文件！
# DO NOT use this file directly in production!
#
# 生产环境应使用:
# - External Secrets Operator
# - Sealed Secrets
# - AWS Secrets Manager
# - Azure Key Vault
# - HashiCorp Vault
# ==========================================================================
apiVersion: v1
kind: Secret
metadata:
  name: sku-service-secrets
  namespace: myplatform
  labels:
    app: sku-service
    version: v1
  annotations:
    description: "示例配置文件，包含占位符值，切勿在生产环境使用"
type: Opaque
stringData:
  # 数据库连接字符串 - 主库 (占位符)
  DB_MASTER_CONNECTION: "Server=mysql-master.myplatform.svc.cluster.local;Database=sku_management;User=app_user;Password=CHANGE_ME_IN_PRODUCTION;"
  
  # 数据库连接字符串 - 从库1 (占位符)
  DB_REPLICA1_CONNECTION: "Server=mysql-replica-1.myplatform.svc.cluster.local;Database=sku_management;User=app_user;Password=CHANGE_ME_IN_PRODUCTION;"
  
  # 数据库连接字符串 - 从库2 (占位符)
  DB_REPLICA2_CONNECTION: "Server=mysql-replica-2.myplatform.svc.cluster.local;Database=sku_management;User=app_user;Password=CHANGE_ME_IN_PRODUCTION;"
  
  # JWT密钥 (占位符 - 生产环境必须替换)
  JWT_SECRET_KEY: "REPLACE_WITH_YOUR_SUPER_SECRET_KEY_THAT_IS_AT_LEAST_32_BYTES!"
  
  # JWT配置
  JWT_ISSUER: "MyPlatform"
  JWT_AUDIENCE: "MyPlatform.Api"
---
# 推荐: 在生产环境中使用 External Secrets Operator 或 Sealed Secrets
# Recommended: Use External Secrets Operator or Sealed Secrets in production
# 
# 示例：使用 External Secrets Operator
# apiVersion: external-secrets.io/v1beta1
# kind: ExternalSecret
# metadata:
#   name: sku-service-secrets
#   namespace: myplatform
# spec:
#   refreshInterval: 1h
#   secretStoreRef:
#     kind: ClusterSecretStore
#     name: vault-backend
#   target:
#     name: sku-service-secrets
#   data:
#     - secretKey: DB_MASTER_CONNECTION
#       remoteRef:
#         key: myplatform/sku-service
#         property: db_master_connection
